package main

// All subcommands now live in dedicated files. The stubs file is retained
// empty as a marker so future contributors know to add new subcommand
// implementations alongside their dedicated *.go file rather than here.
