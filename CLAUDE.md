# Fleet Management Operator

Contributor and agent instructions live in `AGENTS.md`, which Claude Code and Codex both read.
One canonical file means the two cannot drift apart.

Sub-directory `CLAUDE.md` files (`api/v1alpha1/`, `internal/controller/`, `pkg/`,
`charts/fleet-management-operator/`) are still auto-discovered by Claude Code and carry deltas
only.

@AGENTS.md
