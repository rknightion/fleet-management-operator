<!--
Auto-generated by hack/docgen. Do not edit by hand.
Run `make docs` after touching flag declarations in cmd/main.go.
-->

# Manager Flags

The `manager` binary accepts the flags listed below. They are also surfaced
through the Helm chart's `controllers.*` and `fleetManagement.*` value blocks
where applicable. Defaults are conservative: every controller other than
`pipeline` is opt-in.

| Flag | Type | Default | Set by chart? | Description | Source |
| --- | --- | --- | --- | --- | --- |
| `--controller-discovery-max-concurrent` | int | `1` | yes (L111) | Max concurrent reconciles for CollectorDiscovery. Keep at 1: concurrency > 1 triggers multiple ListCollectors calls per poll cycle without benefit. | [cmd/main.go:154](../cmd/main.go:154) |
| `--controller-pipeline-discovery-max-concurrent` | int | `1` | yes (L112) | Max concurrent reconciles for PipelineDiscovery. Keep at 1: concurrency > 1 triggers multiple ListPipelines calls per poll cycle without benefit. | [cmd/main.go:157](../cmd/main.go:157) |
| `--controller-policy-max-concurrent` | int | `4` | yes (L107) | Max concurrent reconciles for RemoteAttributePolicy. Safe to increase: reconciles are pure K8s cache reads with no external API calls. Pipeline and Collector must stay at 1 because they share the Fleet API rate budget. | [cmd/main.go:147](../cmd/main.go:147) |
| `--controller-sync-max-concurrent` | int | `4` | yes (L108) | Max concurrent reconciles for ExternalAttributeSync. Safe to increase: Fetch calls are per-source and do not share external state across reconciles. | [cmd/main.go:151](../cmd/main.go:151) |
| `--controller-sync-target-burst` | int | `4` | yes (L110) | Bucket size for the per-target ExternalAttributeSync limiter. Ignored when --controller-sync-target-rate=0. Default 4 (matching --controller-sync-max-concurrent so a single concurrency generation always passes through immediately). | [cmd/main.go:166](../cmd/main.go:166) |
| `--controller-sync-target-rate` | float64 | `0` | yes (L109) | Per-target rate limit (tokens/sec) applied before each ExternalAttributeSync Source.Fetch call. Two syncs against the same upstream (HTTP host or SQL secret) share a token bucket so MaxConcurrentReconciles cannot stampede a customer-owned source. Zero (default) disables per-target limiting. Set 1 for one fetch/sec/upstream — typically plenty given that sync schedules run every minute or longer. | [cmd/main.go:160](../cmd/main.go:160) |
| `--enable-collector-controller` | bool | `false` | yes (L99) | Enable the Collector reconciler and webhook (manages collector remote attributes). | [cmd/main.go:122](../cmd/main.go:122) |
| `--enable-collector-discovery-controller` | bool | `false` | yes (L102) | Enable the CollectorDiscovery reconciler and webhook (auto-mirrors Fleet Management collectors as Collector CRs). | [cmd/main.go:128](../cmd/main.go:128) |
| `--enable-external-sync-controller` | bool | `false` | yes (L101) | Enable the ExternalAttributeSync reconciler and webhook (HTTP/SQL-backed scheduled attribute pulls). | [cmd/main.go:126](../cmd/main.go:126) |
| `--enable-http2` | bool | `false` | yes (L119) | If set, HTTP/2 will be enabled for the metrics and webhook servers | [cmd/main.go:118](../cmd/main.go:118) |
| `--enable-pipeline-controller` | bool | `true` | yes (L98) | Enable the Pipeline reconciler and webhook. | [cmd/main.go:120](../cmd/main.go:120) |
| `--enable-pipeline-discovery-controller` | bool | `false` | yes (L103) | Enable the PipelineDiscovery controller (polls Fleet Management ListPipelines and creates Pipeline CRs). | [cmd/main.go:130](../cmd/main.go:130) |
| `--enable-policy-controller` | bool | `false` | yes (L100) | Enable the RemoteAttributePolicy reconciler and webhook (bulk attribute assignment by selector). | [cmd/main.go:124](../cmd/main.go:124) |
| `--enable-tenant-policy-enforcement` | bool | `false` | yes (L104) | Enable TenantPolicy CRD validation and enforcement. When set, validating webhooks for Pipeline, RemoteAttributePolicy, and ExternalAttributeSync require that K8s subjects matched by a TenantPolicy include at least one of the policy's required matchers in the CR's matcher set. Default false; existing installs see no behavior change until this flag is set. | [cmd/main.go:132](../cmd/main.go:132) |
| `--fleet-api-burst` | int | `50` | yes (L106) | Fleet Management API rate-limiter burst size. Absorbs startup and post-restart request spikes without changing the sustained RPS ceiling. burst=1 causes livelock at scale: request #(rps*30+1) in a restart wave waits 30s and hits the HTTP timeout, indistinguishable from API outage. | [cmd/main.go:142](../cmd/main.go:142) |
| `--fleet-api-rps` | float64 | `3` | yes (L105) | Fleet Management API sustained rate limit in requests per second. Match this to your Fleet Management server-side api: rate setting. The standard stack default is 3; large or custom deployments may be higher. | [cmd/main.go:138](../cmd/main.go:138) |
| `--health-probe-bind-address` | string | `:8081` | yes (L95) | The address the probe endpoint binds to. | [cmd/main.go:103](../cmd/main.go:103) |
| `--leader-elect` | bool | `false` | conditional (L90) | Enable leader election for controller manager. Enabling this will ensure there is only one active controller manager. | [cmd/main.go:104](../cmd/main.go:104) |
| `--leader-election-lease-duration` | duration | `15 * time.Second` | conditional (L91) | Duration non-leader candidates will wait before forcing leader acquisition. | [cmd/main.go:174](../cmd/main.go:174) |
| `--leader-election-renew-deadline` | duration | `10 * time.Second` | conditional (L92) | Duration the acting leader will retry refreshing leadership before giving up. | [cmd/main.go:176](../cmd/main.go:176) |
| `--leader-election-retry-period` | duration | `2 * time.Second` | conditional (L93) | Duration leader-election clients wait between action attempts. | [cmd/main.go:178](../cmd/main.go:178) |
| `--metrics-bind-address` | string | `0` | conditional (L114, L116) | The address the metrics endpoint binds to. Use :8443 for HTTPS or :8080 for HTTP, or leave as 0 to disable the metrics service. | [cmd/main.go:101](../cmd/main.go:101) |
| `--metrics-secure` | bool | `true` | yes (L118) | If set, the metrics endpoint is served securely via HTTPS. Use --metrics-secure=false to use HTTP instead. | [cmd/main.go:107](../cmd/main.go:107) |
| `--webhook-cert-key` | string | `tls.key` | conditional (L127) | The name of the webhook key file. | [cmd/main.go:112](../cmd/main.go:112) |
| `--webhook-cert-name` | string | `tls.crt` | conditional (L126) | The name of the webhook certificate file. | [cmd/main.go:111](../cmd/main.go:111) |
| `--webhook-cert-path` | string | — | conditional (L121, L123) | The directory that contains the webhook certificate. | [cmd/main.go:110](../cmd/main.go:110) |
| `--webhook-port` | int | `9443` | yes (L97) | The port that the webhook server listens on. | [cmd/main.go:109](../cmd/main.go:109) |

## Chart coverage

The "Set by chart?" column reflects whether the rendered deployment passes the
flag to the manager binary today.

- **yes** — the flag is unconditionally rendered into the deployment `args:` list.
- **conditional** — the flag is only rendered under a Helm `if` guard
  (typical for cert / leader-election / metrics-secure flags). Whether you see
  it on a given install depends on values.
- **no** — the flag is never set by the chart. Either the default is the
  intended value for chart users (and the flag is reserved for `make run`
  workflows or out-of-tree integrations), or the chart is missing coverage.
  `no` rows are worth checking against new feature flags.
