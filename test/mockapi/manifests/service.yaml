apiVersion: v1
kind: Service
metadata:
  name: mock-fleet-api
  labels:
    app: mock-fleet-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mock-fleet-api
