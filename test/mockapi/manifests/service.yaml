apiVersion: v1
kind: Service
metadata:
  name: mock-fleet-api
  labels:
    app: mock-fleet-api
spec:
  type: ClusterIP
  ports:
  - port: 443
    targetPort: 8443
    protocol: TCP
    name: https
  selector:
    app: mock-fleet-api
