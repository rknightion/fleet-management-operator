---
phase: 03-logging-quality
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/controller/errors_test.go
autonomous: true

must_haves:
  truths:
    - "formatConditionMessage returns correct actionable messages for all HTTP status code categories"
    - "formatConditionMessage handles network timeouts and context deadline correctly"
    - "All existing tests continue to pass after logging changes"
    - "Code follows existing conventions (go vet, go fmt, goimports)"
    - "No breaking changes to Pipeline CRD or webhook behavior"
  artifacts:
    - path: "internal/controller/errors_test.go"
      provides: "Unit tests for formatConditionMessage helper"
      contains: "TestFormatConditionMessage"
  key_links:
    - from: "internal/controller/errors_test.go"
      to: "internal/controller/errors.go"
      via: "direct function call"
      pattern: "formatConditionMessage"
---

<objective>
Add unit tests for the formatConditionMessage helper and run quality gates to verify the entire codebase is clean.

Purpose: The formatConditionMessage helper maps errors to user-facing condition messages. Incorrect messages would confuse users debugging pipeline issues. Tests verify all HTTP status codes and error types produce the expected actionable messages. Quality gates ensure no regressions.

Output: Updated errors_test.go with formatConditionMessage tests, passing full test suite, clean go vet/fmt.
</objective>

<execution_context>
@/Users/mbaykara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbaykara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-logging-quality/03-01-PLAN.md
@internal/controller/errors.go
@internal/controller/errors_test.go
@pkg/fleetclient/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for formatConditionMessage helper</name>
  <files>internal/controller/errors_test.go</files>
  <action>
Add a new test function `TestFormatConditionMessage` to `internal/controller/errors_test.go`. This file already uses package `controller` (not `controller_test`) from Phase 2, and already imports `fleetclient` and `testify/assert`.

Use table-driven tests with testify/assert (consistent with existing pattern in errors_test.go).

Test cases for formatConditionMessage:

1. **FleetAPIError 400 (BadRequest)**: Input: FleetAPIError{StatusCode: 400, Message: "invalid config"}. Expected: contains "Configuration validation failed", contains "syntax errors"
2. **FleetAPIError 401 (Unauthorized)**: Input: FleetAPIError{StatusCode: 401, Message: "invalid token"}. Expected: contains "Authentication failed", contains "credentials"
3. **FleetAPIError 403 (Forbidden)**: Input: FleetAPIError{StatusCode: 403, Message: "access denied"}. Expected: contains "Permission denied", contains "pipeline:write"
4. **FleetAPIError 404 (NotFound)**: Input: FleetAPIError{StatusCode: 404, Message: "not found"}. Expected: contains "not found", contains "deleted externally"
5. **FleetAPIError 429 (TooManyRequests)**: Input: FleetAPIError{StatusCode: 429, Message: "rate limited"}. Expected: contains "Rate limited", contains "automatically"
6. **FleetAPIError 500 (InternalServerError)**: Input: FleetAPIError{StatusCode: 500, Message: "internal"}. Expected: contains "unavailable", contains "HTTP 500"
7. **FleetAPIError 502 (BadGateway)**: Input: FleetAPIError{StatusCode: 502, Message: "bad gateway"}. Expected: contains "unavailable", contains "HTTP 502"
8. **FleetAPIError 503 (ServiceUnavailable)**: Input: FleetAPIError{StatusCode: 503, Message: "service unavailable"}. Expected: contains "unavailable", contains "HTTP 503"
9. **FleetAPIError unknown status (418 Teapot)**: Input: FleetAPIError{StatusCode: 418, Message: "teapot"}. Expected: contains "API error", contains "HTTP 418", contains "teapot"
10. **Wrapped FleetAPIError**: Input: fmt.Errorf("wrapped: %w", &FleetAPIError{StatusCode: 400, Message: "invalid"}). Expected: contains "Configuration validation failed" (verifies errors.As works through wrapping)
11. **context.DeadlineExceeded**: Input: context.DeadlineExceeded. Expected: contains "timeout", contains "network connectivity"
12. **Generic error**: Input: fmt.Errorf("something broke"). Expected: contains "Sync failed", contains "controller logs"

Test structure:
```go
func TestFormatConditionMessage(t *testing.T) {
    tests := []struct {
        name     string
        reason   string
        err      error
        contains []string
    }{
        // ... test cases ...
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            result := formatConditionMessage(tt.reason, tt.err)
            for _, substr := range tt.contains {
                assert.Contains(t, result, substr, "message should contain: %s", substr)
            }
        })
    }
}
```

Use "SyncFailed" as the default reason for most test cases. Use "ValidationError" for the 400 case to match real usage.

Add imports needed: "context", "fmt". The file already imports "testing", "github.com/stretchr/testify/assert", and "github.com/grafana/fleet-management-operator/pkg/fleetclient".
  </action>
  <verify>
Run `go test ./internal/controller/... -count=1 -run TestFormatConditionMessage -v` to verify new tests pass.
Run `go test ./... -count=1 -short` to verify ALL tests pass (TEST-05).
Run `go vet ./...` to verify no issues.
Run `goimports -l .` to verify import formatting.
Verify test count: `grep -c "name:" internal/controller/errors_test.go` shows adequate test cases.
  </verify>
  <done>
TestFormatConditionMessage has 12 test cases covering all HTTP status code categories (400, 401, 403, 404, 429, 500, 502, 503, unknown), wrapped errors, context.DeadlineExceeded, and generic errors. All tests pass. All 12 cases verify messages contain actionable guidance strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run quality gates and final verification</name>
  <files></files>
  <action>
This is a verification-only task. No files are modified.

Run the following quality gate checks:

1. **Compilation**: `go build ./...` must succeed
2. **Vet**: `go vet ./...` must report no issues
3. **Format**: `goimports -l .` must report no files (or fix any that need it with `goimports -w .`)
4. **Tests**: `go test ./... -count=1` must pass ALL tests (not just -short)
5. **No breaking changes** (QUAL-01):
   - Verify Pipeline CRD types unchanged: `git diff HEAD -- api/v1alpha1/` should show no changes
   - Verify webhook unchanged: `git diff HEAD -- api/v1alpha1/*webhook*` should show no changes
   - Verify no changes to config/crd: `git diff HEAD -- config/crd/` should show no changes
6. **Convention compliance** (QUAL-02):
   - Verify structured logging (no fmt.Sprintf in log calls): `grep -rn 'log\.\(Info\|Error\|V\).*fmt.Sprintf' internal/controller/` should have no matches
   - Verify errors use fmt.Errorf with %w: `grep -rn 'fmt.Errorf' internal/controller/` should show %w wrapping
7. **Code comments** (QUAL-03):
   - Verify comments exist on new functions: `grep -B1 "func formatConditionMessage\|func loggerFor" internal/controller/errors.go` should show doc comments
8. **Requirement coverage check**:
   - LOG-01: Verify structured logging. `grep -c '"namespace"' internal/controller/pipeline_controller.go` matches number of log statements
   - LOG-02: Verify condition messages. `grep -c "formatConditionMessage" internal/controller/pipeline_controller.go` shows usage
   - LOG-03: Verify transition logging. `grep -c "transitioned" internal/controller/pipeline_controller.go` shows at least 2 (success + error)
   - LOG-04: Verify namespace/name. Same as LOG-01 check

If any check fails, fix the issue. If goimports reports files, run `goimports -w .` and note the fix.
  </action>
  <verify>
All 8 quality gate checks pass. Output the results of each check.
  </verify>
  <done>
All quality gates pass: compilation, vet, format, full test suite, no breaking changes, convention compliance, code comments present, and all LOG/QUAL requirements verified. Phase 3 is complete.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors
2. `go vet ./...` reports no issues
3. `goimports -l .` reports no files needing formatting
4. `go test ./... -count=1` all tests pass (including new formatConditionMessage tests)
5. `git diff HEAD -- api/v1alpha1/` shows no CRD/webhook changes (QUAL-01)
6. No fmt.Sprintf in log statements (LOG-01, QUAL-02)
7. formatConditionMessage used in condition messages (LOG-02)
8. Transition logging present (LOG-03)
9. All log statements have namespace/name (LOG-04)
10. Code comments on new functions (QUAL-03)
</verification>

<success_criteria>
- 12 test cases for formatConditionMessage covering all error categories
- Full test suite passes (including Phase 1 and Phase 2 tests)
- go vet and goimports clean
- No changes to api/v1alpha1/ or config/crd/ (no breaking changes)
- All Phase 3 requirements (LOG-01 through LOG-04, QUAL-01 through QUAL-04, TEST-05) verified
</success_criteria>

<output>
After completion, create `.planning/phases/03-logging-quality/03-02-SUMMARY.md`
</output>
