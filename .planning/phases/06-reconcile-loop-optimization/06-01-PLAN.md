---
phase: 06-reconcile-loop-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/pipeline_controller.go
  - internal/controller/reconcile_audit_test.go
autonomous: true

must_haves:
  truths:
    - "Every Get/Update in reconcile loop has a justification comment explaining WHY it is needed"
    - "All status updates use Status().Update(), never Update() on full resource"
    - "ObservedGeneration pattern correctly skips reconcile when spec is unchanged"
    - "No redundant Get after Create/Update operations (returned object is reused)"
    - "Finalizer removal makes exactly two K8s API calls: one cached Get, one Update"
  artifacts:
    - path: "internal/controller/pipeline_controller.go"
      provides: "Reconcile justification comments on every Get/Update operation"
      contains: "Reconcile:"
    - path: "internal/controller/reconcile_audit_test.go"
      provides: "AST-based and structural verification tests for reconcile loop optimization"
      exports: ["TestStatusUpdatesUseSubresource", "TestNoRedundantGetAfterUpsert", "TestObservedGenerationGuard", "TestFinalizerMinimalAPICalls", "TestReconcileJustificationDocumentation"]
  key_links:
    - from: "internal/controller/reconcile_audit_test.go"
      to: "internal/controller/pipeline_controller.go"
      via: "AST parsing and source file reading"
      pattern: "parser\\.ParseFile.*pipeline_controller\\.go"
---

<objective>
Audit and document every Kubernetes API call in the reconcile loop, then write verification tests proving all five RECON requirements are met.

Purpose: Ensure the reconcile loop makes minimal, justified API server calls. Every Get/Update must have a documented reason, status updates must use the correct subresource method, and the ObservedGeneration pattern must be verified to prevent unnecessary reconciliation.

Output: Justification comments on all K8s API calls in the controller, plus an executable test suite that prevents regressions.
</objective>

<execution_context>
@/Users/mbaykara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbaykara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-informer-cache-audit/05-01-SUMMARY.md
@internal/controller/pipeline_controller.go
@internal/controller/cache_audit_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reconcile justification comments to every Get/Update operation</name>
  <files>internal/controller/pipeline_controller.go</files>
  <action>
Add "Reconcile:" prefix comments to every Kubernetes API call in the reconcile loop. These are SEPARATE from the existing "Cache:" comments (which explain cache behavior). The "Reconcile:" comments explain WHY each API call is necessary and cannot be eliminated.

The controller has exactly these K8s API calls (each needs a "Reconcile:" justification):

1. `r.Get(ctx, req.NamespacedName, pipeline)` in Reconcile() (line ~134):
   - Already has Cache: comment. Add Reconcile: comment above or after it.
   - Justification: "Reconcile: Required entry point - fetch the resource that triggered reconciliation. Cannot be eliminated; this is the standard controller-runtime pattern."

2. `r.Update(ctx, pipeline)` for finalizer addition (line ~154):
   - Justification: "Reconcile: Finalizer must be persisted before any Fleet Management API call. Returns immediately to let the watch re-trigger reconciliation with finalizer present."

3. `r.Update(ctx, pipeline)` for finalizer removal in reconcileDelete() (line ~222):
   - Justification: "Reconcile: Finalizer removal is the final K8s API call in deletion. Once removed, the API server garbage-collects the resource. No subsequent Get is needed."

4. `r.Status().Update(ctx, pipeline)` in updateStatusSuccess() (line ~407):
   - Justification: "Reconcile: Status subresource update after successful Fleet Management sync. Uses Status().Update() (not Update()) to avoid triggering a spec-change watch event."

5. `r.Status().Update(ctx, pipeline)` in updateStatusError() (line ~477):
   - Justification: "Reconcile: Status subresource update to record error condition. Uses Status().Update() to avoid spec-change watch event. Original error is preserved for exponential backoff."

Important implementation notes:
- Do NOT modify any code logic. This is documentation-only.
- Keep existing "Cache:" comments intact. Add "Reconcile:" as a separate comment line.
- Use the exact "Reconcile:" prefix for grep-ability and audit tooling (consistent with the "Cache:" pattern from Phase 5).
- Also add a package-level section to the existing "Cache Usage Audit" block at the top of the file, adding a "Reconcile Loop Audit" section that summarizes:
  - Total K8s API calls: 5 (1 Get, 2 Update, 2 Status().Update())
  - Happy path: 3 calls (Get + UpsertPipeline via Fleet API + Status().Update())
  - Finalizer add: 2 calls (Get + Update), returns immediately
  - Delete path: 3 calls (Get + DeletePipeline via Fleet API + Update to remove finalizer)
  - ObservedGeneration skip: 1 call (Get only, no further processing)
  </action>
  <verify>
Run `grep -c "Reconcile:" internal/controller/pipeline_controller.go` and confirm at least 5 matches (one per API call) plus the audit summary.
Run `go build ./...` to confirm no syntax errors.
  </verify>
  <done>
Every Get/Update/Status().Update() call in the reconcile loop has a "Reconcile:" justification comment. A package-level "Reconcile Loop Audit" summary exists. Build succeeds with no code changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write reconcile loop verification tests</name>
  <files>internal/controller/reconcile_audit_test.go</files>
  <action>
Create `internal/controller/reconcile_audit_test.go` with verification tests for RECON-02 through RECON-05. Use the same patterns established in `cache_audit_test.go` (AST parsing, source file reading, testify/assert). Use standard `testing` package (not Ginkgo).

Tests to implement:

1. **TestStatusUpdatesUseSubresource** (RECON-02):
   - Parse `pipeline_controller.go` with go/ast.
   - Walk AST to find all call expressions where the method is `Update`.
   - For each `Update` call, check the receiver expression:
     - If the receiver is a `Status()` call (i.e., `r.Status().Update()`), this is correct for status updates.
     - If the receiver is `r` directly (i.e., `r.Update()`), verify it is NOT operating on a status field. The two direct `r.Update()` calls are for finalizer operations, which is correct.
   - Assert that zero `r.Update()` calls exist that modify status fields.
   - Implementation approach: Count calls matching `Status().Update()` pattern (should be exactly 2) and `r.Update()` pattern (should be exactly 2, for finalizer add and remove). Any additional `r.Update()` calls should fail the test.

2. **TestNoRedundantGetAfterUpsert** (RECON-04):
   - Parse `pipeline_controller.go` with go/ast.
   - Find all functions that call `UpsertPipeline` (reconcileNormal and handleAPIError).
   - Within those functions, verify there is NO `r.Get()` call AFTER the `UpsertPipeline` call.
   - Approach: For each function body, track whether UpsertPipeline has been seen; after that point, assert no Get() calls exist.
   - Also verify that the return value of UpsertPipeline is used (assigned to a variable, not discarded).

3. **TestObservedGenerationGuard** (RECON-03):
   - Read `pipeline_controller.go` as text.
   - Verify the ObservedGeneration check exists in the Reconcile function:
     - Contains `pipeline.Status.ObservedGeneration == pipeline.Generation` comparison
     - The check occurs AFTER finalizer addition but BEFORE reconcileNormal call
   - Verify ObservedGeneration is SET in both success and error paths:
     - `updateStatusSuccess` contains `pipeline.Status.ObservedGeneration = pipeline.Generation`
     - `updateStatusError` contains `pipeline.Status.ObservedGeneration = pipeline.Generation`

4. **TestFinalizerMinimalAPICalls** (RECON-05):
   - Parse `pipeline_controller.go` with go/ast.
   - Find the `reconcileDelete` function.
   - Count the number of K8s API calls (Get, Update, Status().Update(), Create, Delete, Patch, List) within that function body.
   - Assert exactly 1 `Update` call (finalizer removal). The `r.Get()` at the top of Reconcile() is not inside reconcileDelete, so reconcileDelete itself should have exactly 1 K8s API call.
   - Note: `r.FleetClient.DeletePipeline()` is a Fleet Management API call, NOT a K8s API call, so it should not be counted.

5. **TestReconcileJustificationDocumentation** (documentation enforcement):
   - Read `pipeline_controller.go` as text.
   - Verify all "Reconcile:" documentation markers are present (similar to TestCacheAuditDocumentation).
   - Check for "Reconcile Loop Audit" summary section.
   - Count total "Reconcile:" markers (should be at least 5 + summary).

Each test should have clear failure messages explaining what the requirement is and why the test failed, following the pattern in cache_audit_test.go.
  </action>
  <verify>
Run `go test ./internal/controller/ -run "TestStatusUpdatesUseSubresource|TestNoRedundantGetAfterUpsert|TestObservedGenerationGuard|TestFinalizerMinimalAPICalls|TestReconcileJustificationDocumentation" -v` and confirm all 5 tests pass.
Run `go test ./internal/controller/ -v` to confirm all existing tests still pass.
  </verify>
  <done>
Five verification tests pass confirming: (1) all status updates use Status().Update(), (2) no redundant Get after UpsertPipeline, (3) ObservedGeneration guard is correctly placed and set in both paths, (4) reconcileDelete makes exactly 1 K8s API call, (5) all reconcile justification documentation is present. All existing tests continue to pass.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds (no syntax errors from documentation changes)
2. `grep -c "Reconcile:" internal/controller/pipeline_controller.go` returns >= 6
3. `go test ./internal/controller/ -run "TestStatusUpdatesUseSubresource" -v` passes
4. `go test ./internal/controller/ -run "TestNoRedundantGetAfterUpsert" -v` passes
5. `go test ./internal/controller/ -run "TestObservedGenerationGuard" -v` passes
6. `go test ./internal/controller/ -run "TestFinalizerMinimalAPICalls" -v` passes
7. `go test ./internal/controller/ -run "TestReconcileJustificationDocumentation" -v` passes
8. `go test ./internal/controller/ -v` passes (all existing tests unbroken)
</verification>

<success_criteria>
- RECON-01: Every Get/Update in reconcile loop has "Reconcile:" justification comment
- RECON-02: TestStatusUpdatesUseSubresource proves exactly 2 Status().Update() and exactly 2 r.Update() (finalizer only)
- RECON-03: TestObservedGenerationGuard proves guard exists in correct position and is set in both paths
- RECON-04: TestNoRedundantGetAfterUpsert proves no Get() follows UpsertPipeline
- RECON-05: TestFinalizerMinimalAPICalls proves reconcileDelete has exactly 1 K8s API call
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-reconcile-loop-optimization/06-01-SUMMARY.md`
</output>
