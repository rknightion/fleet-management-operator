---
phase: 02-controller-error-handling
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/controller/errors_test.go
  - internal/controller/pipeline_controller_test.go
autonomous: true

must_haves:
  truths:
    - "Unit tests prove updateStatusError returns original error when status update fails"
    - "Unit tests prove updateStatusError returns Requeue on status conflict"
    - "Unit tests prove validation errors do not trigger retry"
    - "Unit tests prove external deletion detection has single-retry limit"
    - "Unit tests prove isTransientError classifies FleetAPIError correctly"
    - "Unit tests prove shouldRetry returns false for validation errors"
  artifacts:
    - path: "internal/controller/errors_test.go"
      provides: "Unit tests for error classification helpers"
      contains: "TestIsTransientError"
    - path: "internal/controller/pipeline_controller_test.go"
      provides: "Unit tests for updateStatusError and handleAPIError fixes"
      contains: "TestUpdateStatusError"
  key_links:
    - from: "internal/controller/errors_test.go"
      to: "internal/controller/errors.go"
      via: "direct function calls"
      pattern: "isTransientError\\|shouldRetry"
    - from: "internal/controller/pipeline_controller_test.go"
      to: "internal/controller/pipeline_controller.go"
      via: "PipelineReconciler method calls"
      pattern: "updateStatusError\\|handleAPIError"
---

<objective>
Add unit tests for all Phase 2 controller error handling fixes: error classification helpers, updateStatusError original error preservation, status conflict handling, and external deletion recursion limit.

Purpose: Verify the bug fixes work correctly, prevent regressions, and document expected error behavior through tests.

Output: Two test files with comprehensive coverage of error classification and controller error handling.
</objective>

<execution_context>
@/Users/mbaykara/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbaykara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-controller-error-handling/02-RESEARCH.md
@.planning/phases/02-controller-error-handling/02-01-SUMMARY.md
@internal/controller/errors.go
@internal/controller/pipeline_controller.go
@internal/controller/pipeline_controller_test.go
@internal/controller/suite_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for error classification helpers</name>
  <files>internal/controller/errors_test.go</files>
  <action>
Create a new file `internal/controller/errors_test.go` in package `controller` with standard Go tests (NOT Ginkgo -- use `testing` package with `testify/assert` for table-driven tests, matching the pattern from `pkg/fleetclient/client_test.go`).

IMPORTANT: This file uses `package controller` (NOT `controller_test`), so it can access the unexported `isTransientError` and `shouldRetry` functions directly.

**TestIsTransientError** (table-driven, ~10 test cases):
- FleetAPIError with 500 (Internal Server Error) -> true
- FleetAPIError with 502 (Bad Gateway) -> true
- FleetAPIError with 503 (Service Unavailable) -> true
- FleetAPIError with 429 (Too Many Requests) -> true
- FleetAPIError with 408 (Request Timeout) -> true
- FleetAPIError with 400 (Bad Request) -> false
- FleetAPIError with 401 (Unauthorized) -> false
- FleetAPIError with 404 (Not Found) -> false
- Wrapped FleetAPIError (via `fmt.Errorf("wrapped: %w", apiErr)`) with 500 -> true (proves errors.As works through wrapping)
- `context.Canceled` error -> false
- Generic `errors.New("network error")` -> true (unknown errors default to transient)
- `nil` error: skip or handle gracefully (the function should not be called with nil, but document this)

Each test case struct: `{name string, err error, want bool}`.

**TestShouldRetry** (table-driven, ~5 test cases):
- Validation error reason with transient error -> false (validation always permanent)
- SyncFailed reason with transient FleetAPIError (500) -> true
- SyncFailed reason with permanent FleetAPIError (400) -> false
- DeleteFailed reason with transient FleetAPIError (503) -> true
- SyncFailed reason with generic error -> true (unknown defaults to transient)

Each test case struct: `{name string, err error, reason string, want bool}`.

Use the `reasonValidationError`, `reasonSyncFailed`, `reasonDeleteFailed` constants from pipeline_controller.go (accessible since same package).

Imports needed: `context`, `errors`, `fmt`, `net/http`, `testing`, `github.com/stretchr/testify/assert`, and the fleetclient package.

Add standard copyright header.
  </action>
  <verify>Run `go test ./internal/controller/... -run "TestIsTransientError|TestShouldRetry" -v -count=1` -- all test cases must pass.</verify>
  <done>errors_test.go has TestIsTransientError (~10 cases) and TestShouldRetry (~5 cases), all passing.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for updateStatusError and handleAPIError fixes</name>
  <files>internal/controller/pipeline_controller_test.go</files>
  <action>
Add new test functions to the EXISTING `internal/controller/pipeline_controller_test.go` file. These tests use Ginkgo since the file already uses Ginkgo framework (it imports gomega and ginkgo/v2).

Add a new `Context("Controller Error Handling", func() { ... })` block INSIDE the existing top-level `Describe` block (or as a new `Describe` block at the same level). This block should contain direct unit tests that do NOT require envtest -- they test methods directly on PipelineReconciler with a fake Kubernetes client.

**Important setup pattern:** Use `sigs.k8s.io/controller-runtime/pkg/client/fake` to create fake clients. The fake client builder needs the scheme registered. For status update conflict simulation, create a custom wrapper that intercepts `Status().Update()` calls.

Create a helper type for testing status update failures:

```go
// statusErrorClient wraps a fake client to inject status update errors
type statusErrorClient struct {
    client.Client
    statusUpdateErr error
}

type statusErrorWriter struct {
    client.StatusWriter
    err error
}

func (c *statusErrorClient) Status() client.StatusWriter {
    if c.statusUpdateErr != nil {
        return &statusErrorWriter{StatusWriter: c.Client.Status(), err: c.statusUpdateErr}
    }
    return c.Client.Status()
}

func (w *statusErrorWriter) Update(ctx context.Context, obj client.Object, opts ...client.SubResourceUpdateOption) error {
    return w.err
}
```

**Test: updateStatusError preserves original error** (TEST-02, STAT-02):
- Create a PipelineReconciler with the statusErrorClient configured to return a generic error on status update.
- Create a Pipeline object with the fake client (must exist in the fake client's store).
- Call `updateStatusError(ctx, pipeline, reasonSyncFailed, originalErr)` where `originalErr = errors.New("API connection failed")`.
- Assert: returned error IS `originalErr` (not the status update error).
- Assert: returned Result is `ctrl.Result{}` (no requeue).
- Assert: pipeline.Status.ObservedGeneration equals pipeline.Generation.
- Assert: Ready condition is set to False with reason SyncFailed.

**Test: updateStatusError conflict triggers requeue** (STAT-01):
- Create a PipelineReconciler with the statusErrorClient configured to return `apierrors.NewConflict(...)`.
- Call `updateStatusError(ctx, pipeline, reasonSyncFailed, originalErr)`.
- Assert: returned error is `nil`.
- Assert: returned Result.Requeue is `true`.

**Test: updateStatusError validation error does not retry**:
- Create a PipelineReconciler with a working fake client (status update succeeds).
- Create a Pipeline object.
- Call `updateStatusError(ctx, pipeline, reasonValidationError, validationErr)`.
- Assert: returned error is `nil` (no retry).
- Assert: returned Result is `ctrl.Result{}` (no requeue).

**Test: handleAPIError 404 with existing ID recreates inline** (REC-01):
- Create a PipelineReconciler with mock FleetClient that returns 404 first, then succeeds on second UpsertPipeline call. Use a counter in the mock to control this.
- Set `pipeline.Status.ID = "old-id-123"`.
- Call `handleAPIError(ctx, pipeline, &fleetclient.FleetAPIError{StatusCode: 404, Operation: "UpsertPipeline", Message: "not found"})`.
- Assert: the mock's UpsertPipeline was called (recreation happened).
- Assert: either success result or the mock's second call result is returned.

**Test: handleAPIError 404 with empty ID fails immediately** (REC-01, TEST-04):
- Create a PipelineReconciler with a fake client.
- Set `pipeline.Status.ID = ""` (already tried recreation).
- Call `handleAPIError(ctx, pipeline, &fleetclient.FleetAPIError{StatusCode: 404, Operation: "UpsertPipeline", Message: "not found"})`.
- Assert: returned error is NOT nil (it gives up, returns error for backoff).
- Assert: error message contains "recreation failed".

Register the scheme with the fake client builder using:
```go
import "k8s.io/client-go/kubernetes/scheme"
// ... in test setup:
fleetmanagementv1alpha1.AddToScheme(scheme.Scheme)
fakeClient := fake.NewClientBuilder().WithScheme(scheme.Scheme).WithStatusSubresource(&fleetmanagementv1alpha1.Pipeline{}).WithObjects(pipeline).Build()
```

The `.WithStatusSubresource()` call is critical -- without it, the fake client's `Status().Update()` will be a no-op.

Add standard copyright header to any new code sections.
  </action>
  <verify>Run `go test ./internal/controller/... -v -count=1` -- all tests (existing + new) must pass. Run `go vet ./internal/controller/...` -- must pass.</verify>
  <done>New test cases for updateStatusError (3 scenarios) and handleAPIError 404 (2 scenarios) all pass, covering TEST-02, TEST-04, STAT-01, STAT-02, and REC-01 requirements.</done>
</task>

</tasks>

<verification>
1. `go test ./internal/controller/... -v -count=1` passes all tests (existing + new)
2. `go test ./... -count=1 -short` passes entire project test suite
3. `go vet ./...` passes with no issues
4. `grep -c "func Test" internal/controller/errors_test.go` shows at least 2 test functions
5. `grep -c "updateStatusError\|handleAPIError" internal/controller/pipeline_controller_test.go` confirms new test coverage
</verification>

<success_criteria>
- Error classification helpers have comprehensive unit tests covering FleetAPIError, wrapped errors, context.Canceled, and unknown errors
- updateStatusError is tested for: original error preservation, conflict requeue, and validation no-retry
- handleAPIError 404 is tested for: single-retry recreation and immediate failure on empty ID
- All existing tests continue to pass (no regressions)
- Requirements TEST-02 and TEST-04 are fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-controller-error-handling/02-02-SUMMARY.md`
</output>
